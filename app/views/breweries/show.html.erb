<p id="notice"><%= notice %></p>
<h2><%= @brewery.name %></h2> 
<i>Established year <%= @brewery.year %></i>
<p>
  <strong>Number of beers:</strong>
  <%= @brewery.beers.count %>
</p>
<p>
 <% @brewery.beers.each do |beer| %>
   <%= link_to beer.name, beer %>
 <% end %>
</p>

<p>
<% if @brewery.ratings.empty? %>
 Beers have not yet been rated!
<% else %>
 Beers have <%= pluralize(@brewery.ratings.count, 'rating') %>, average <%= @brewery.average_rating %>
<% end %>
</p>

<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>
